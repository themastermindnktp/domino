# DOMINO

Install dependencies
```shell
npm install
```

Compile contracts
```shell
npx hardhat compile
```

Test
```shell
npx hardhat test
```

Run local node
```shell
npx hardhat node
```

Deploy local
```shell
npx hardhat run scripts/deploy.js
```

