const { ethers } = require('hardhat');
const { expect } = require('chai');
const { mine } = require('@nomicfoundation/hardhat-network-helpers');

const { nullAddress, maxUInt256 } = require('./helper');

describe('3. Auction', async () => {

});
